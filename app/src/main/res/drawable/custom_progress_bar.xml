<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/progress">
        <rotate
            android:fromDegrees="270"
            android:pivotX="50%"
            android:pivotY="50%"
            android:toDegrees="270">
            <shape
                android:innerRadiusRatio="2.5"
                android:shape="ring"
                android:thicknessRatio="35.0">
                <gradient
                    android:centerColor="@color/navigation_bar_color"
                    android:endColor="@color/navigation_bar_color"
                    android:startColor="@color/navigation_bar_color"
                    android:type="sweep" />
            </shape>
        </rotate>
    </item>
    <item android:id="@android:id/secondaryProgress">
        <rotate
            android:fromDegrees="270"
            android:pivotX="50%"
            android:pivotY="50%"
            android:toDegrees="270">
            <shape
                android:innerRadiusRatio="2.5"
                android:shape="ring"
                android:thicknessRatio="35.0">
                <gradient
                    android:centerColor="@color/pink"
                    android:endColor="@color/pink"
                    android:startColor="@color/pink"
                    android:type="sweep" />
            </shape>
        </rotate>
    </item>
</layer-list>    